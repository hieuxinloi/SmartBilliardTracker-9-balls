services:
  # Original video processing API
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
      - ./models:/app/models
      # Mount backend code for hot reload (development only)
      - ./backend:/app/backend
      - ./detect_collision.py:/app/detect_collision.py
      - ./ball_detect.py:/app/ball_detect.py
      - ./utils.py:/app/utils.py
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
    networks:
      - billiard-network

  # New real-time game system API
  backend-game:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8001:8001"
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
      - ./models:/app/models
      - ./match_history:/app/match_history
      # Mount backend code for hot reload (development only)
      - ./backend:/app/backend
      - ./detect_collision.py:/app/detect_collision.py
      - ./ball_detect.py:/app/ball_detect.py
      - ./utils.py:/app/utils.py
    environment:
      - PYTHONUNBUFFERED=1
    command: python backend/main_game.py
    networks:
      - billiard-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend
      - backend-game
    networks:
      - billiard-network

networks:
  billiard-network:
    driver: bridge

volumes:
  uploads:
  outputs:
  match_history:
